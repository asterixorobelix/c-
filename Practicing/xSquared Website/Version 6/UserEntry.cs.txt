using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.Data.Entity;//required for Entity framework

namespace Squared.Models
{
    public class UserrEntry //Each instance of a UserrEntry object will correspond to a row within a database table. Each property will correspond to a column in the table.
    {
        public int ID { get; set; }//A property called ID
        public string name { get; set; }
    }

    public class UserrEntryDBContext : DbContext //The UserrEntryDBContext class handles fetching, storing, and updating UserrEntries class instances in a database. The UserrEntryDBContext derives from the DbContext base class provided by the Entity Framework.
    {
        public DbSet<UserrEntry> UserrEntries { get; set; }
    }
}